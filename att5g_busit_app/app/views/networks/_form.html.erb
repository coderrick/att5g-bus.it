<%= form_with(model: network, local: true) do |form| %>
  <% if network.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(network.errors.count, "error") %> prohibited this network from being saved:</h2>

      <ul>
        <% network.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bus_id %>
    <%= form.number_field :bus_id %>
  </div>

  <div class="field">
    <%= form.label :5g %>
    <%= form.check_box :5g %>
  </div>

  <div class="field">
    <%= form.label :station_id %>
    <%= form.number_field :station_id %>
  </div>

  <div class="field">
    <%= form.label :bandwidth %>
    <%= form.text_field :bandwidth %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
